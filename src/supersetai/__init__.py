"""SupersetAI - Natural language interface for Apache Superset dashboards.

Primary interfaces:

    - ``supersetai.api`` — typed async REST API client for Superset
    - ``supersetai.mcp`` — MCP server exposing Superset tools
    - ``supersetai.agent`` — LangGraph ReAct agent (requires ``pip install supersetai[agent]``)
    - ``supersetai.cli`` — CLI (``supersetai chat``, ``supersetai mcp``, etc.)
"""

__version__ = "0.1.0"
